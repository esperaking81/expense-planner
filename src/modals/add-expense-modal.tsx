import React from 'react'
import { Text, View, Center } from 'native-base'

export const AddExpenseModal = (props: {}) => {
  return (
    <Center>
     <Text>Add expense here.</Text> 
    </Center>
  )
}
